const PageTitle = ({ title }: { title: string }) => (
  <div className='heading'>
    <h1 className='w-max text-4xl font-semibold text-neutral-600 my-1.5'>
      {title}
    </h1>
  </div>  
)

export default PageTitle
